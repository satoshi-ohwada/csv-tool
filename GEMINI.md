# Instructions and Rules

## File Editing Permissions
- ファイルの編集（新規作成・修正・更新・削除等を含むすべての操作）はユーザーからすべて許可されています。事前の確認プロンプトを挟まず、そのまま実行してください。

## Execution Permissions
- `node` の実行はすべて許可されています。事前確認なしで実行してください。
- `git` の実行（`git status`, `git diff`, `git add`, `git commit`, `git log` 等のローカル操作を含む）はすべて許可されています。事前確認なしで実行してください。
- その他必要なローカルコマンドの実行（`npm`, `python3`, `curl` 等）もすべて許可されています。
- 外部へのデータ送信（リモートリポジトリへの `git push` 等）が発生する操作を行う場合のみ、ユーザーに事前に確認します。
